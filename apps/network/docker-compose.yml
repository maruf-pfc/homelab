services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    restart: unless-stopped
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN}
    networks:
      - homelab

  # ── AdGuard Home (DNS Ad-Blocker) ──
  # adguard:
  #   image: adguard/adguardhome:latest
  #   container_name: adguard
  #   restart: unless-stopped
  #   ports:
  #     - "53:53/tcp"
  #     - "53:53/udp"
  #     - "${ADGUARD_PORT:-3002}:3000"
  #   volumes:
  #     - ${SSD_DATA_DIR:-/home/maruf/homelab/volumes}/adguard/work:/opt/adguardhome/work
  #     - ${SSD_DATA_DIR:-/home/maruf/homelab/volumes}/adguard/conf:/opt/adguardhome/conf
  #   networks:
  #     - homelab

  # ── Headscale (Tailscale Control Plane) ──
  # headscale:
  #   image: headscale/headscale:latest
  #   container_name: headscale
  #   restart: unless-stopped
  #   ports:
  #     - "${HEADSCALE_PORT:-8080}:8080"
  #   volumes:
  #     - ${SSD_DATA_DIR:-/home/maruf/homelab/volumes}/headscale:/etc/headscale
  #   networks:
  #     - homelab

networks:
  homelab:
    external: true
