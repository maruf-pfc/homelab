# ============================================================
# Category 4: Network & Ingress
#
# NOTE: Cloudflare Tunnel is NOT managed here as a container.
# It is installed directly on the host as a system daemon:
#   /usr/local/bin/cloudflared (v2025.7.0)
#
# The tunnel is configured and managed via the Cloudflare
# dashboard at: https://one.dash.cloudflare.com
#
# To check tunnel status on the host:
#   systemctl status cloudflared
#   journalctl -u cloudflared -f
# ============================================================

services:

  # ── AdGuard Home (DNS Ad-Blocker) — disabled by default ──
  # adguard:
  #   image: adguard/adguardhome:latest
  #   container_name: adguard
  #   restart: unless-stopped
  #   ports:
  #     - "53:53/tcp"
  #     - "53:53/udp"
  #     - "${ADGUARD_PORT:-3002}:3000"
  #   volumes:
  #     - ${SSD_DATA_DIR:-/home/maruf/homelab/volumes}/adguard/work:/opt/adguardhome/work
  #     - ${SSD_DATA_DIR:-/home/maruf/homelab/volumes}/adguard/conf:/opt/adguardhome/conf
  #   networks:
  #     - homelab

networks:
  homelab:
    external: true
